---
title: API Reference
description: ArtemisKit API documentation - redirects to SDK
---

import { Aside, Card, CardGrid } from '@astrojs/starlight/components';

# API Reference

<Aside type="note">
  The API documentation has moved to the SDK section for better organization.
</Aside>

<CardGrid>
  <Card title="SDK Documentation" icon="document">
    Full programmatic API including evaluation, Guardian Mode, and test matchers.

    [View SDK docs →](/docs/sdk/)
  </Card>
  <Card title="Evaluation API" icon="rocket">
    `kit.run()`, `kit.validate()`, `kit.compare()`, `kit.redteam()`, `kit.stress()`

    [View Evaluation API →](/docs/sdk/evaluation/)
  </Card>
  <Card title="Guardian Mode" icon="shield">
    Runtime protection with semantic validation and injection detection.

    [View Guardian docs →](/docs/sdk/guardian/)
  </Card>
  <Card title="Scenario Builders" icon="puzzle">
    Programmatic scenario construction with TypeScript builders.

    [View Builders →](/docs/sdk/builders/)
  </Card>
</CardGrid>

## Quick Reference

### Core Package (`@artemiskit/core`)

```typescript
import { createAdapter } from '@artemiskit/core';

const client = await createAdapter({
  provider: 'openai',
  apiKey: process.env.OPENAI_API_KEY,
});

const result = await client.generate({
  prompt: 'What is 2+2?',
  maxTokens: 100,
});
```

### SDK Package (`@artemiskit/sdk`)

```typescript
import { ArtemisKit } from '@artemiskit/sdk';

const kit = new ArtemisKit({
  provider: 'openai',
  model: 'gpt-4',
});

const results = await kit.run({
  scenario: './scenarios/quality.yaml',
});
```

### Guardian Mode

```typescript
import { createGuardian } from '@artemiskit/sdk/guardian';

const guardian = createGuardian({
  mode: 'selective',
  contentValidation: { strategy: 'semantic' },
});

const protectedClient = guardian.protect(client);
```

## See Also

- [SDK Overview](/docs/sdk/) — Full SDK documentation
- [Core Concepts](/docs/concepts/) — Scenarios, expectations, providers
- [Examples](/docs/examples/) — Practical recipes
